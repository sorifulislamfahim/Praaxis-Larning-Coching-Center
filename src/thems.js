import { createGlobalStyle } from "styled-components"

export const lightThems = {
    body:"#fff",
    fontColor: "#000"
}

export const drakThems = {
    body: "#000",
    fontColor: "#fff"
}

// export GlobalStyles = createGlobalStyle`

//     body{
//         backgroundColor: ${(props) => props.them.body}
//     }
// `;